extends CharacterBody2D

"""
var dir: Vector2 = Vector2.ZERO
const DECAY = 0.1  # How fast the velocity slows down

func shoot(direction: Vector2, power: float) -> void:
	# Set your velocity based on sling direction and power (strength)
	dir = direction.normalized() * power

func _physics_process(delta):
	velocity.x = 100  # constant right speed
	velocity.y = 0
	move_and_slide()
"""
